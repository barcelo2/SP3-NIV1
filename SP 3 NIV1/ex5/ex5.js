"use strictly";
let nota = prompt(" Quina nota has rebut a l'examen? ");

if (nota < 5) {
  alert(` Ohh, has suspés l'examen amb la nota ${nota}`);
} else {
  alert(`Molt bé, has aprovat l'examen amb la nota ${nota}`);
}
